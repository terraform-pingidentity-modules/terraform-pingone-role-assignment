#
# IF this files is missing, then no Ping Library items will be created
#

hcl-examples:
  examples/users:
    ping-library::
      name: "Terraform Role Assignment Module Example"
      short-description: "This is the card short description"
      long-description: "This is the longer description.  It is optional and if not, set, the short-description will be shown"
      meta-data
  
    #base-path: examples                                         # base path of the example files - DEFAULT: examples
    include-files: ["main.tf", "subdir2/providers.tf"]          # list of files to include. DEFAULT: list eveything (main.tf if found (expanded), is first, and then alphabetical)
  
    #show-all-files: false                       # true or false - DEFAULT: false
    #hide-all-files: false                       # true or false - DEFAULT: true
    #exclude-files: ["exluded-file.tf"]          # list of files to exclude. Default [] - Overrides show/hide files
  
  
  example2:
    name: "Example2"
    short-description: "This is the card short description"
    long-description: "This is the longer description.  It is optional and if not, set, the short-description will be shown"
  
    show-all-files: false                       # true or false - DEFAULT: false
    hide-all-files: false                       # true or false - DEFAULT: true
    include-files: ["main.tf"]                  # list of files to include. Default [] - Overrides show/hide files
    exclude-files: []                           # list of files to exclude. Default [] - Overrides show/hide files

bookmars-example:

js-snippets:

documentation-example:
